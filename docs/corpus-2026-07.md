# Validation corpus — 2026-07-27

Nine open-hardware KiCad boards screened with `faraday_cli` at commit `bb01e4e`+.
The corpus exists to find **importer and rule bugs on real layouts**; the finding
counts below are screening-tier output, not a judgement on these designs.

Boards are fetched on demand (`scripts/fetch_corpus.py`), not vendored — only the
two fixtures under `cpp/tests/fixtures/real/` are committed, with attribution.

## Results

| board | kind | layers | segs | stackup | findings | top rules | SW | diff | ms |
|---|---|---|---|---|---|---|---|---|---|
| `hackrf-one` | SDR (RF front end) | 4 | 3817 | board-file | 200+584 | plane-crossing 129, coupled-run 69, sparse-reference 2 | 0 | 10 | 521 |
| `mppt-2420-hc` | solar charge controller (buck) | 4 | 1263 | user:4layer | 167 | coupled-run 63, 3w 50, plane-crossing 49 | 2 | 1 | 71 |
| `vesc` | motor drive (BLDC inverter) | 4 | 1671 | user:4layer | 200+345 | coupled-run 104, plane-crossing 48, 3w 48 | 0 | 3 | 44 |
| `orangecrab` | ECP5 FPGA + DDR3 | 6 | 6919 | user:6layer | 200+280 | coupled-run 86, 3w 76, plane-crossing 38 | 0 | 12 | 271 |
| `mppt-2420-lc` | solar charge controller (buck) | 2 | 1668 | user:2layer | 200+436 | coupled-run 109, 3w 68, plane-crossing 23 | 1 | 5 | 52 |
| `bms-c1` | battery management | 4 | 1097 | board-file | 101 | plane-crossing 33, coupled-run 31, 3w 31 | 0 | 3 | 98 |
| `glasgow` | debug/interface tool | 4 | 4715 | board-file | 200+654 | coupled-run 122, 3w 70, plane-crossing 8 | 0 | 20 | 259 |
| `fomu` | tiny FPGA | 4 | 579 | user:4layer | 42 | coupled-run 29, 3w 8, no-reference-plane 4 | 0 | 1 | 17 |
| `ulx3s` | FPGA board | 4 | 4811 | board-file | 200+2137 | coupled-run 153, plane-crossing 37, 3w 10 | 0 | 14 | 336 |

`+N` = findings past the 200 cap (reported, never silently dropped).
**9/9 screened, worst case 521 ms** — the whole-board budget holds at 6 layers
and ~7000 segments.

## Bugs the corpus found (all fixed, all now regression-tested)

**1. False negative — a poured layer's own routing was skipped.** `bms-c1` has
58–89 % pour on *all four* layers, so every layer classified as a plane and every
segment was excluded: Faraday reported **0 findings on a 1097-segment board**. A
layer can be a reference *and* carry routing; the two concepts are now separate.
After the fix: 101 findings. This was the worst class of bug in the tool — a
confident clean verdict on an unanalysed board. `meta.planes[].routedMm` now
reports analysed length per layer so a "0 findings" verdict is auditable.

**2. Parse failure — vias inheriting drill from the netclass.** `vesc` has 7 vias
with no `(drill ...)`; the importer required it and refused the whole board. Drill
feeds no rule, so it is now optional, `0` means unspecified (never invented), and
`board.viasWithoutDrill` states the count.

**3. Unanalysable 6-layer boards.** Only 2- and 4-layer built-in stackups existed,
so `orangecrab` (6 layers) had no legal option. `default-<N>layer` now synthesises
a symmetric 1.6 mm FR4 stack for any N — still a user-selected approximation,
still stated in the report as `user:default-6layer`.

## Known gaps this corpus exposed (not yet fixed)

- **Inverter phase nodes are not found.** `vesc` reports 0 switch nodes: the L+Q
  heuristic looks for an inductor, and a motor drive's phase node runs FETs →
  connector (the motor is the inductance). Buck/boost converters are covered
  (`mppt-2420-hc`, `mppt-2420-lc`); inverter topologies need a separate rule.
- **Finding cap dominates dense boards.** Six of nine hit the 200 cap, `ulx3s`
  discarding 2137. The cap is reported, but per-rule interleaving or per-rule
  caps would give a more useful top-200 than "whatever ranked highest".
- **`hackrf-one` produces 129 individual hard-break rows.** These are genuine (no
  plane covers), so the aggregation added for the *detour* case does not apply —
  but 129 rows is still a poor read. A per-(layer, plane) roll-up for hard breaks
  is worth considering.

## Reproducing

```sh
python3 scripts/fetch_corpus.py /tmp/faraday-corpus   # fetches from GitHub
python3 scripts/run_corpus.py  /tmp/faraday-corpus    # screens, writes JSON
```

Board licences vary (GPL-2.0, CERN-OHL-W-2.0, CC-BY-SA-4.0, 0BSD, and some
unstated); `manifest.json` records each. They are test inputs, not redistributed.
